{% extends 'base-admin.html.twig' %}


{% block title %}Les fournisseurs{% endblock %}


{% block notifications %}
  {% for message in app.flashes('success') %}
    <div class="alert alert-success fade in">
      <a href="#" class="close" data-dismiss="alert">&times;</a>
      <strong>Succès</strong><br>
      {{ message|raw }}
    </div>
  {% endfor %}
  {% for message in app.flashes('warning') %}
    <div class="alert alert-warning fade in">
      <a href="#" class="close" data-dismiss="alert">&times;</a>
      <strong>Attention !!!</strong><br>
      {{ message|raw }}
    </div>
  {% endfor %}
  {% for message in app.flashes('danger') %}
    <div class="alert alert-danger fade in">
      <a href="#" class="close" data-dismiss="alert">&times;</a>
      <strong>Erreur</strong><br>
      {{ message|raw }}
    </div>
  {% endfor %}
{% endblock %}


{% block body %}
  <div class="fh5co-narrow-content">
    <h2 class="fh5co-heading animate-box" data-animate-effect="fadeInLeft">Fournisseurs</h2>
    <a href="{{ path('provider.add') }}" class="btn btn-primary">Ajouter un fournisseur</a>
    <div class="row row-bottom-padded-md">
      {% for provider in providers %}
          <div class="col-md-3 col-sm-6 col-padding animate-box" data-animate-effect="fadeInLeft">
            <div class="blog-entry">
              <a href="#" class="blog-img"><img src="{{ asset('images/img-1.jpg') }}" class="img-responsive" alt="Free HTML5 Bootstrap Template by FreeHTML5.co"></a>
              <div class="desc">
                <h3><a href="#">{{ provider.firstname }} {{ provider.lastname }}</a></h3>
                <span>{{ provider.country.name }}</span>
                <p>Forunisseur</p>
                <a href="{{ path('product.edit', {id: provider.id}) }}" class="lead">Modifier<i class="icon-arrow-right3"></i></a>
              </div>
            </div>
          </div>
      {% endfor %}
    </div>
  </div>
{% endblock %}
