{% extends 'base.html.twig' %}

{% block title %}Les ventes du jour{% endblock %}


{% block notifications %}
{% endblock %}


{% block body %}
<div class="fh5co-narrow-content">
  <h2 class="fh5co-heading animate-box" data-animate-effect="fadeInLeft">Les ventes du {{ date|date('d-m-Y') }}</h2>
  <a href="javascript:history.back()" class="btn btn-primary sm">
    <span class="lnr lnr-arrow-left"></span>&nbsp;Retour
  </a><br><br>
  <div class="row">
    <table class="table table-sm">
      <thead>
        <tr>
          <th>#</th>
          <th>Date</th>
          <th>Total des ventes</th>
          <th>Actions</th>
        </tr>
      </thead>
      <tbody>
        {% set i = 0 %}
        {% for item in ventes %}
          {% set i = i + 1 %}
          <tr>
            <td>{{ i }}</td>
            <td>{{ item.date|date('d-m-Y') }}</td>
            <td>{{ item.totalAmount|number_format(0, ',', ' ') }} F</td>
            <td>
              <a href="{{ path('customer.order.details', {id: item.id}) }}" class=" circle arrow small">Plus de détails</a>
            </td>
          </tr>
        {% endfor %}
      </tbody>
    </table>
  </div>
</div>
{% endblock %}
